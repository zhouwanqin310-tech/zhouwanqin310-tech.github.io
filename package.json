{
  "name": "wanqin-zhou-academic-homepage",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "dev": "eleventy --serve --port=8088",
    "build": "eleventy",
    "test": "node --test tests/*.test.js",
    "check": "npm test && npm run build && node tests/check-site.js"
  },
  "devDependencies": { "@11ty/eleventy": "3.1.6" }
}
